<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="summoner">
    <select id="insertSummonerData">
        insert into summoner(summoner_name, id, accountid, puuid, profileiconid, summonerlevel, revisiondate)
            values(#{summonerName}, #{id}, #{accountId}, #{puuid}, #{profileiconId}, #{summonerLevel}, #{revisionDate})
    </select>
</mapper>